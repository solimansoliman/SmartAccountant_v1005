# 🔑 دليل توليد JWT Key

## ما هو JWT Key؟

JWT Key هو مفتاح سري يُستخدم لـ:
- ✅ تشفير وتوقيع التوكنات (tokens)
- ✅ التحقق من صحة التوكنات عند كل طلب
- ✅ حماية API من الوصول غير المصرح

---

## ⚠️ قواعد أمان مهمة

| القاعدة | السبب |
|---------|-------|
| 🔒 لا تشارك المفتاح | أي شخص يعرفه يمكنه تزوير توكنات |
| 🔒 لا تحفظه في Git | سيكون مكشوفاً للجميع |
| 🔒 كل سيرفر = مفتاح مختلف | إذا تسرب أحدهم، الباقي آمن |
| 🔒 الطول 64+ حرف | مفاتيح قصيرة سهلة الكسر |

---

## 🛠️ طرق التوليد

### الطريقة 1: PowerShell (الأسهل)

```powershell
[Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Max 256 }) -as [byte[]])
```

**النتيجة:**
```
a3KsW16i1P69je0Gyp045yeCstkvxxVyjlU9Oqb5VP5tcqs2QcIM9v2zAVfefPiIjvahTI3LpK3IYEyMLy6zRg==
```

---

### الطريقة 2: PowerShell (مع Cryptography)

```powershell
$rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()
$bytes = New-Object byte[] 64
$rng.GetBytes($bytes)
[Convert]::ToBase64String($bytes)
```

---

### الطريقة 3: C# Code

```csharp
using System.Security.Cryptography;

var key = new byte[64];
RandomNumberGenerator.Fill(key);
var base64Key = Convert.ToBase64String(key);
Console.WriteLine(base64Key);
```

---

### الطريقة 4: Online (غير موصى به للإنتاج)

- https://generate.plus/en/base64
- https://www.allkeysgenerator.com/

⚠️ **تحذير**: لا تستخدم مواقع الإنترنت للإنتاج الحقيقي!

---

### الطريقة 5: Linux/Mac Terminal

```bash
openssl rand -base64 64
```

---

## 📋 كيفية الاستخدام

### 1. ولّد المفتاح

```powershell
[Convert]::ToBase64String((1..64 | % { Get-Random -Max 256 }) -as [byte[]])
```

### 2. انسخ النتيجة

### 3. ضعها في `appsettings.Production.json`

```json
{
  "Jwt": {
    "Key": "المفتاح_الذي_نسخته_هنا",
    "Issuer": "SmartAccountant",
    "Audience": "SmartAccountantUsers",
    "ExpiryInMinutes": 60,
    "RefreshExpiryInDays": 7
  }
}
```

---

## 🔄 متى تغيّر المفتاح؟

| الحالة | الإجراء |
|--------|---------|
| 🚨 تسرب المفتاح | غيّره فوراً! |
| 📅 كل 6 أشهر | تغيير دوري للأمان |
| 👤 موظف غادر | إذا كان يعرف المفتاح |
| 🔓 اختراق محتمل | احتياطياً |

**ملاحظة**: عند تغيير المفتاح، كل المستخدمين سيحتاجون تسجيل دخول جديد.

---

## 🏗️ البيئات المختلفة

```
┌────────────────────────────────────────────────┐
│  Development (التطوير)                         │
│  Key: Dev_abc123xyz...                         │
│  ملف: appsettings.Development.json            │
├────────────────────────────────────────────────┤
│  Local (المحلي)                                │
│  Key: Local_def456uvw...                       │
│  ملف: appsettings.Local.json                  │
├────────────────────────────────────────────────┤
│  Production (الإنتاج)                          │
│  Key: Prod_ghi789rst... (الأهم!)              │
│  ملف: appsettings.Production.json             │
└────────────────────────────────────────────────┘
```

---

## 💡 نصائح إضافية

### 1. استخدم Environment Variables (الأفضل)

بدلاً من حفظ المفتاح في الملف:

**Windows:**
```powershell
[Environment]::SetEnvironmentVariable("JWT_KEY", "your_key_here", "Machine")
```

**في appsettings.json:**
```json
{
  "Jwt": {
    "Key": "${JWT_KEY}"
  }
}
```

### 2. استخدم Azure Key Vault أو AWS Secrets Manager

للمشاريع الكبيرة والشركات.

---

## 📞 أمثلة جاهزة

### مفتاح للتطوير (يمكنك استخدامه):
```
DevKey_THIS_IS_ONLY_FOR_DEVELOPMENT_NOT_FOR_PRODUCTION_USE_64_CHARS_MINIMUM!!
```

### توليد مفتاح للإنتاج (نفذ هذا الأمر):
```powershell
[Convert]::ToBase64String((1..64 | % { Get-Random -Max 256 }) -as [byte[]])
```

---

**تاريخ الإنشاء:** 2026-01-27
