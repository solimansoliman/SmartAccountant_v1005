# 🗄️ دليل إعداد قاعدة البيانات

**آخر تحديث:** مارس 2026  
**النسخة:** 1.0.5  
**الحالة:** ✅ مكتمل ونشط

---

## 📋 الملفات النشطة - الطريقة الموحدة والشاملة

جميع ملفات الإعداد توجد في مجلد `database/active/`:

**هناك ملفان شاملان فقط يغطيان كل شيء:**

### خطوة 1️⃣: إنشاء قاعدة البيانات والجداول (السكيمة)
**الملف:** `SCHEMA_COMPLETE.sql`
- ✅ ينشئ قاعدة البيانات `SmartAccountant_v1005_DB`
- ✅ ينشئ 15 جدول مع جميع الأعمدة الصحيحة
- ✅ ينشئ جميع العلاقات (Foreign Keys)
- ✅ ينشئ الفهارس والقيود
- ✅ يدعم العربية بشكل كامل (Arabic_100_CI_AS)

```powershell
# شغل هذا الأمر في PowerShell
sqlcmd -S "(localdb)\mssqllocaldb" -f 65001 -i "database/active/SCHEMA_COMPLETE.sql"
```

### خطوة 2️⃣: إدراج جميع البيانات الأساسية
**الملف:** `DATA_COMPLETE.sql`
- ✅ العملات: 5 عملات (SAR, AED, EGP, USD, EUR)
- ✅ الخطط: 3 خطط اشتراك (أساسية، محترفة، مؤسسات)
- ✅ الأدوار: 4 أدوار نظام (Owner, Admin, Manager, Staff)
- ✅ الصلاحيات: 52 صلاحية متقدمة
- ✅ الدول: 20 دولة عربية
- ✅ المحافظات والمدن: البيانات الجغرافية كاملة
- ✅ حساب إدارة + مستخدم مسؤول
- ✅ 6 إعدادات نظامية

```powershell
# شغل هذا الأمر بعد الخطوة 1 مباشرة
sqlcmd -S "(localdb)\mssqllocaldb" -d "SmartAccountant_v1005_DB" -f 65001 -i "database/active/DATA_COMPLETE.sql"
```

---

## 🔐 بيانات المستخدم الافتراضي

| البيان | القيمة |
|-------|--------|
| اسم المستخدم | `admin` |
| كلمة المرور | `admin123` |
| الدور | Administrator (مسؤول) |
| الحساب | شركة التجريب (Demo Company) |
| الصلاحيات | كاملة (جميع الصلاحيات) |
| الحالة | نشط (Active) |
| العملة الافتراضية | SAR (ريال سعودي) |

---

## ✅ اختبار الإعداد

```sql
-- تحقق من وجود قاعدة البيانات
SELECT NAME FROM sys.databases WHERE NAME = 'SmartAccountant_v1005_DB';

-- عد الجداول (يجب أن يكون 15)
SELECT COUNT(*) AS [عدد الجداول] FROM sys.tables WHERE database_id = DB_ID('SmartAccountant_v1005_DB');

-- تحقق من المستخدم الأدمن
SELECT [Id], [Username], [Email], [FullName], [IsActive] FROM [Users] WHERE [Username] = 'admin';

-- تحقق من العملات المضافة (يجب أن يكون 5)
SELECT COUNT(*) AS [عدد العملات] FROM [Currencies];

-- تحقق من الخطط المضافة (يجب أن يكون 3)
SELECT COUNT(*) AS [عدد الخطط] FROM [Plans];

-- تحقق من الأدوار (يجب أن يكون 4)
SELECT COUNT(*) AS [عدد الأدوار] FROM [Roles];

-- تحقق من الصلاحيات (يجب أن يكون 52)
SELECT COUNT(*) AS [عدد الصلاحيات] FROM [Permissions];
```

---

## 🔄 إعادة تعيين النظام بالكامل

إذا احتجت لحذف وإعادة إنشاء كل شيء:

```powershell
# 1. حذف قاعدة البيانات من SQL Server
sqlcmd -S "(localdb)\mssqllocaldb" -Q "DROP DATABASE SmartAccountant_v1005_DB"

# 2. إعادة تشغيل الخطوتين (1 و 2) أعلاه بالترتيب
```

---

## 📝 ملاحظات مهمة

- ⚠️ **النسخ الاحتياطية:** تأكد من عمل نسخة احتياطية قبل حذف البيانات
- 🔒 **الأمان:** غيّر كلمة المرور الافتراضية (`admin123`) بعد الإعداد مباشرة
- 🌍 **اللغة:** قاعدة البيانات تدعم العربية والإنجليزية بشكل كامل
- 💰 **العملات:** يمكن إضافة عملات أخرى من خلال واجهة الإدارة
- ⏱️ **الوقت:** كل ملف يأخذ 5-10 ثوانٍ في التنفيذ
- 📊 **الحجم:** ملف SCHEMA ~500 سطر، ملف DATA ~2000 سطر
